#include <stdio.h>
#include <stdlib.h>
#include "main.h"
#include <string.h>
#include <ctype.h>
/**
 * _isdigit - tests if the int is a number
 * @c: the int to be tested
 * Return: int
 */
int _isdigit(int c)
{
	return (isdigit(c));
}
