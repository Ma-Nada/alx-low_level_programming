#include "main.h"
/**
 *  * _isupper - test if the char is in uppercase
 *   *@c: the character to be tested
 *    * Return - 0 Successful
 *     */
int _isupper(int c)
{
	if ( c >= 41 && c <= 90)
	return (0);
	return (1);
}
