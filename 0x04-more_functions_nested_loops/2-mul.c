#include "main.h"
/**
 * mul - Entry point
 *
 * Description: 'the program's description'
 * @a: describe the parameter
 * @b: integer
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
